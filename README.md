# Movie Poster Generator Telegram Bot

A Pyrogram-based Telegram bot that automatically generates stylized movie posters (1280×720) whenever a movie is sent to a specified database channel or to the bot directly.

## Features

- 🎬 Automatic movie detection from text, files, and images
- 🎨 Beautiful poster generation with blur backgrounds
- 📊 TMDB integration for movie metadata
- 💾 MongoDB caching for performance
- 📤 Automatic posting to movie channel
- 🎯 Inline movie selection with multiple options
- 🧹 Automatic cleanup of generated files

## Setup

1. **Clone the repository**
2. **Install dependencies:**
   ```bash
   pip install -r requirements.txt
